language: python
python:
  - 2.7

 
before_install:
   # add for profile
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq


# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install -r requirements-test.txt
  - pip install coveralls
# add for profile
  - sudo apt-get install -qq g++-4.8
  - export CXX="g++-4.8"
  - pip install nose-cprof
  - pip install properform

# command to run tests
script:
  - nosetests --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_abydos_a283c2275e_1
  - python -m properform Push 47.88.50.110:15554 asdf_abydos_a283c2275e_1 ./profile_abydos_a283c2275e_1



after_success:
  - coveralls
 #2017-08-12 20:45:14 modified at 0 #2017-08-12 20:45:21 modified at 1